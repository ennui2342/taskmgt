FROM python:3.12-slim AS base
WORKDIR /app
COPY pyproject.toml .
COPY src/ src/
RUN pip install --no-cache-dir -e .
ENV PORT=8081
EXPOSE 8081
CMD ["python", "-m", "taskapi"]

FROM base AS test
RUN pip install --no-cache-dir -e ".[test]"
COPY tests/ tests/
CMD ["pytest", "tests/api", "-v"]
