apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: taskmgt
  namespace: taskmgt
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: tasks.k8s.ecafe.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
  tls:
    - hosts:
        - tasks.k8s.ecafe.org
      secretName: tasks.k8s.ecafe.org-production
